apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-node-deployment
  labels:
    app: edge-node
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edge-node
  template:
    metadata:
      labels:
        app: edge-node
    spec:
      containers:
      - name: edge-server
        image: nginx:alpine
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: edge-node-service
spec:
  type: ClusterIP
  selector:
    app: edge-node
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80